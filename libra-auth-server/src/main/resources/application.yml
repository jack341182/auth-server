spring:
  datasource:
    username: ${MYSQL_USERNAME:root}
    password: ${MYSQL_PASSWORD:123456}
    url: jdbc:mysql://${MYSQL_SERVER_HOST:10.1.10.75}:3306/zc_auth_server?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=utf-8&autoReconnect=true&useSSL=false
    # 使用druid数据源
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect
  application:
    name: auth-server
  redis:
    host: ${REDIS_HOST:10.1.10.239}
    database: ${REDIS_DATABASE:3}
  wechat:
    trucker:
      app-id: ${TRUCKER_APP_ID:wx3c6860f872cb2cfc}
      app-secret: ${TRUCKER_APP_SECRET:0acd1a92aee027749eff19d953aee562}
  messages:
    basename: classpath:messages
  jackson:
    time-zone: GMT+8
    locale: zh_CN
logging:
  level:
    org.springframework.security: info
    com.kybb: debug
    com.kybb.fiter: info
#    org.springframework.security.web: debug
